# Handoff — pi-search-multi v1.2.0

**Date:** 2026-05-10T19:54 UTC
**Branch:** `main` at `2647d09`
**npm:** `pi-search-multi@1.2.0`

---

## What was done this session

### Research phase
- Used **scout** subagent to deep-analyze the codebase (architecture, backend interface, config model, integration points, constraints)
- Used **researcher** subagent for external context (pi extension API stability, backend health, ddgs v8 status, Brave tier change, pricing shifts, Typebox migration)
- Synthesized findings and resolved 6 clarification questions with the maintainer

### Implementation — v1.1.0
1. **Perplexity Sonar backend** (`searchPerplexity()`) — calls `/chat/completions`, extracts citations as results, unlimited free tier
2. **SearXNG backend** (`searchSearXNG()`) — self-hosted metasearch, `instanceUrl` config field, optional Bearer auth
3. **DuckDuckGo async refactor** — replaced blocking `execSync` + temp files with `spawn("python3", ["-c", script])`, properly abortable via `AbortSignal`, 30s timeout
4. **Config deep merge** — `loadConfig()` now merges per-backend instead of shallow-replacing, so global backends survive project config overrides
5. **Brave tier updated** — "2000 free/mo" → "metered billing ~$5/mo credit" everywhere (docs, `/search-setup`, comments)
6. **Benchmark updated** — removed stale `bing` backend, added Perplexity + SearXNG tests, DDG test uses `spawn` instead of `execSync`
7. **All docs synced** — README, package.json, github-meta.md, search.json.example, reddit-post.md

### Merge — PR #2: Combine mode (v1.2.0)
- Merged `pungggi`'s PR #2 adding a `combine` boolean parameter to `web_search`
- When enabled, queries ALL backends in parallel, merges & deduplicates by URL, labels each result with source backend
- Resolved merge conflicts with v1.1.0:
  - README header: merged "11 backends" + "combined search"
  - Added `perplexity`/`searxng` to the hardcoded `backendLabel` map in `formatCombinedResults()`
- Published to npm as `pi-search-multi@1.2.0`

### Repository housekeeping
- Updated GitHub repo **About** description (11 backends, combine mode)
- Added topics: `perplexity`, `searxng`, `combine-mode`
- Updated `.gitignore` to exclude session artifacts (`context.md`, `research.md`, unrelated extensions)
- Clean npm tarball (8 files, 84 KB unpacked)

---

## Current state

| File | Lines | What it contains |
|------|-------|------------------|
| `extensions/pi-search.ts` | 1257 | 11 backends + combine mode + 2 commands + session lifecycle |
| `benchmark/benchmark.mjs` | ~340 | Tests all 11 backends, generates markdown report |
| `README.md` | ~190 | Full docs: 11 backends, combine mode, config, security |
| `package.json` | v1.2.0 | 8 files in npm tarball |

### 11 backends
| # | Backend | Auth | Tier |
|---|---------|------|------|
| 1 | DuckDuckGo | None (pip ddgs) | Free, rate-limited |
| 2 | Marginalia | Public key (shared) | Free, rate-limited |
| 3 | Serper | API key | 2500 free/mo |
| 4 | Tavily | API key | 1000 free/mo |
| 5 | Exa | API key | 10 QPS free |
| 6 | Brave | API key | Metered ~$5/mo |
| 7 | LangSearch | API key | Genuinely free |
| 8 | Firecrawl | API key | 500 free credits |
| 9 | WebSearchAPI | API key | 2000 free credits |
| 10 | Perplexity Sonar | API key | Unlimited free |
| 11 | SearXNG | Instance URL | Self-hosted, free |

---

## Possible next work items

These are **not committed tasks** — ideas surfaced during research that could be pursued:

1. **Parallel fan-out in auto mode** — PR #2's combine mode is opt-in (`combine=true`). Could make auto mode do a small parallel fan-out (e.g., 2 backends) by default instead of sequential fallback.
2. **SearXNG public instances** — Add docs or a config shorthand for popular public SearXNG instances (from searx.space).
3. **Result re-ranking in combine mode** — After URL dedup, results are in arbitrary order. Could re-rank by benchmark quality scores per backend.
4. **DuckDuckGo P2P cache** — ddgs v8.x supports P2P distributed cache. Could document or integrate.
5. **Quality-based backend ordering** — Auto mode could reorder backends by benchmark quality score instead of config order.

---

## Next session tips

- **Working directory:** `/mnt/data/dev/projects/pi-search-multi`
- **Subagents available:** `scout` (codebase recon), `researcher` (web research), `planner` (implementation plans), `reviewer` (code review), `worker` (implementation)
- **Skills available:** `search-first` (research-before-coding), `karpathy-guidelines` (surgical changes), `github-markdown` (GFM rules), `recursivemas` (multi-agent framework)
- **To resume:** A subagent can read this handoff + the project README + `extensions/pi-search.ts` to get full context
- **Key files for context:**
  - `extensions/pi-search.ts` — the entire extension (backends, combine mode, commands, lifecycle)
  - `benchmark/benchmark.mjs` — standalone benchmark
  - `.pi/search.json` — live API keys (gitignored)
  - `search.json.example` — example config template
